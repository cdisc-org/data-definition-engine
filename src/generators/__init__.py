"""DDE generators: programs that turn a DDS JSON file into study artifacts.

``define/`` predates this package marker and uses flat top-level imports, so it is run
with its own directory as the working directory and is not imported as
``generators.define``. ``crf/`` is a real subpackage.
"""
