# DTA Logical Data Model — Discovery Questions

## 1. Logical data model for DTA (LinkML)

1. What is the model primarily for — validation contract, transformation source, or semantic/interop layer?
2. Entity decomposition and grain — flat-typed or nested/relational? What is the grain?
3. The triple unit-block (PLR*/CVU*/SIU*) — normalize or keep parallel?
4. Scope of v0 — core slice or full 60 variables?
5. Codelist binding — bind enums to CDISC CT, and from what source?
6. Deliverable bundle — doc only, schema only, or full bundle?
7. Which LinkML targets must this compile to — JSON Schema, SHACL, Pydantic?
8. Identifier strategy — natural keys or surrogate keys?
9. Cardinality rules?

